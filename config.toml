baseurl = "https://stoic-cori-a2fab2.netlify.app/"
title = "運用０円の静的サイトブログ"
languageCode = "ja-jp"
DefaultContentLanguage = "ja-jp"
paginate = "10" # Number of posts per page
theme = "hugo-dpsg"

[Author] # Used in authorbox
  name = "KEISUKE SAKAGAMI"
  bio = "2020.7Web系企業転職 / 2020.1～3プログラミングスクール / 2019.10～2020.1某自動車工場勤務 / 2018.10～2019.10無農薬野菜農家研修 / 2001.4～2018.9地方公務員(道路行政/建設WaterFallですべての職務を経験）"
  avatar = "img/myimage.jpg"  #StaticSiteGeneratorBlog/themes/hugo-dpsg/static/img/myimage.jpg

[Params]
  description = "Welcome to our scout group!" # Site description. Used in meta description
  copyright = "DGSP local group" # Footer copyright holder, otherwise will use site title
  opengraph = true # Enable OpenGraph if true
  schema = true # Enable Schema
  twitter_cards = true # Enable Twitter Cards if true
  readmore = false # Show "Read more" button in list if true
  authorbox = true # Show authorbox at bottom of pages if true
  toc = true # Enable Table of Contents
  pager = true # Show pager navigation (prev/next links) at the bottom of pages if true
  indexPager = true # Show pager navigation on index page
  post_meta = ["author", "date", "categories", "translations"] # Order of post meta information
  mainSections = ["post", "blog", "news"] # Specify section pages to show on home page and the "Recent articles" widget
  dateformat = "02.01.2006" # Change the format of dates

[Params.style.vars]
  highlightColor = "#003056" # Override main theme color

[Params.logo]
  image = "" # Logo image. Path relative to "static"
  header = "img/myheader.jpg" # Header image. Path relative to "static"
  title = "運用０円の静的サイトブログ" # Logo title, otherwise will use site title
  subtitle = "Welcome to Static site blog with 0 yen operation" # Logo subtitle

[Params.sidebar]
  home = "right" # Configure layout for home page
  list = "left"  # Configure layout for list pages
  single = false # Configure layout for single pages
  # Enable widgets in given order
  widgets = ["search", "recent", "categories", "taglist", "social", "languages"]
  # alternatively "ddg-search" can be used, to search via DuckDuckGo
  # widgets = ["ddg-search", "recent", "categories", "taglist", "social", "languages"]

[Params.widgets]
  recent_num = 5 # Set the number of articles in the "Recent articles" widget
  tags_counter = false # Enable counter for each tag in "Tags" widget

[Params.footer]
  text = "[Imprint and Privacy](/imprint)" # Extra text in footer row, understands markdown

# Custom menu items, normally controlled via front matter in /content files
[menu]
  [[menu.main]]
    identifier = "blog_index"
    name = "ブログ記事一覧"
    url = "/"
    weight = 1

  [[menu.main]]
    identifier = "about_me"
    name = "自己紹介"
    url = "/about/"
    weight = 2
